#include <stdio.h> 
#include <math.h> // we add a new libreary.
int main (){
    int x;
    printf ("Enter your no :");
    scanf ("%d",&x);
    float y = sqrt(x);
    printf ("The squar root of given no is = %f",y);
        return 0;
}